# Core OPC UA Client Library (async)
asyncua>=1.0.0

# Logging
loguru>=0.7.0
tqdm>=4.65.0

# Certificate generation (for generate_certificates.py)
cryptography>=41.0.0

# Testing Dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1

# Code Quality & Linting
ruff>=0.1.0
black>=23.0.0
mypy>=1.0.0

# Type Stubs
types-cryptography>=3.3.0

# lxml is required by mypy to generate HTML reports
lxml>=4.9.0