# Core OPC UA Client Library (async)
asyncua>=1.0.0

# Logging
loguru>=0.7.0

# Certificate generation (for generate_certificates.py)
cryptography>=41.0.0

# Development Dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
mypy>=1.0.0